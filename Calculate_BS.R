#!/usr/bin/env Rscript
# DEPRECATED: this script has been split into two entry points.
#   * micro-domains (diagonal-only data) -> Calculate_BS_micro.R
#   * large domains  (full ICE2 matrices) -> Calculate_BS_large.R
# Use BS.sh --method micro|large instead. Safe to delete this file.
stop("Calculate_BS.R is deprecated. Use BS.sh --method micro|large (see README).")
